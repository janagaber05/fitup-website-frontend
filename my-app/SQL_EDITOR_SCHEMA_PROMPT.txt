================================================================================
FITUP — Supabase SQL prompts (simplified, per page)
================================================================================
The large single prompt was **split** into small files you can paste one at a time.

OPEN THIS FOLDER
----------------
my-app/supabase/prompts/

START HERE
----------
0) tables_columns_rows.txt       — index → rows_columns__*.txt (one file per page)
1) README.txt                    — run order
2) 00_supabase_rules_once.txt   — rules for every script
3) foundation__profiles.txt     — /profile
4) foundation__form_submissions.txt — Contact + Partner + Join Us (+ optional home)
5) foundation__gyms.txt        — /platform listings (optional)

Then use **page__contact.txt**, **page__partner.txt**, **page__join_us.txt**, etc.
for page-specific seeds and tweaks only.

LIST TABLES / COLUMNS / ROWS (SQL Editor)
-----------------------------------------
**sql_editor__rows_columns.sql** — paste into the SQL Editor and Run: a single SELECT
(one result grid: table, column, type, nullable, default, PK columns, approx rows, RLS).

Natural-language “prompts” do not run in the query box. For AI assistants use
**supabase/prompts/for_supabase_ai_chat_only.txt** (not pasted as SQL).

FULL PRODUCT (classes, bookings, billing)
-----------------------------------------
See foundation__operations_full_app.txt — use when you build beyond the marketing site,
and still add tables in small steps.

================================================================================
